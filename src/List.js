import React from 'react'
import MyCheckbox from './MyCheckbox.js'

function List(props)
{
    // return (
    //     <div>
    //         data = Array.from(props.src);
    //         props.src.map((text, i) => {return <MyCheckbox depth={i} label={text} />});
            
    //     </div>
    // )
}

export default List